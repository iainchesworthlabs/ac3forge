# Name,     Type, SubType,  Offset,   Size,  Flags
# partitions.csv with a larger application partition, for an ESP32-P4
# (sdkconfig.p4). Sized ahead of a measured image rather than after one: this
# build adds espressif/esp_wifi_remote and espressif/esp_hosted over the plain
# esp_wifi the S3 and C6 images link - a host-side RPC and SDIO transport on
# top of the Sendspin player, not just a driver swap - so the S3's 1.39 MB
# image and even the C6's 1.62 MB (partitions_c6.csv's own reason for
# existing) are not a safe ceiling to assume here. 4 MB leaves comfortable
# headroom either way; correct it against the real number the first
# `idf.py build` for this target reports if it does not fit. Every board
# this project has flashed this partition table to has at least 16 MB.
nvs,        data, nvs,      0x9000,   0x6000,
phy_init,   data, phy,      0xf000,   0x1000,
factory,    app,  factory,  0x10000,  0x400000,
audio,      data, 0x40,     ,         0x40000,
storage,    data, fat,      ,         0x40000,
