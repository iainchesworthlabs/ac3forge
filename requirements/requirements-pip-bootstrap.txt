# Hand-maintained, not a pip-compile output: pip-compile treats pip itself as
# one of the "unsafe" packages (alongside setuptools and wheel) and drops it
# from --generate-hashes locks unless --allow-unsafe is passed, so it cannot
# live in requirements-lint.txt/-pytest.txt/etc. alongside the packages those
# actually pin.
#
# ci.yml's script-lint job and wheels.yml's test-the-wheel job each upgrade
# the venv's bootstrap pip to a known version before running their real
# --require-hashes install, and that upgrade step was itself unpinned by hash
# - version-pinned only, via `pip install "pip==X.Y.Z"`. That is exactly what
# OSSF Scorecard's Pinned-Dependencies check flags (surfaced here as GitHub
# code scanning alerts #112 and #113). This file closes the gap: both jobs
# install pip via `--require-hashes -r requirements/requirements-pip-bootstrap.txt`
# instead. Bump the version and hashes here, in one place, when it needs to move.
#
# pip 24.3.1 was the first version pinned here, and dependency-review.yml
# promptly failed the PR that added this file over it: five moderate-severity
# advisories against pip itself (tar/zip extraction and path-traversal
# issues), all fixed by 26.2.0 - a bare `pip install "pip==X.Y.Z"` shell
# command was invisible to the dependency graph, so those had been running
# in CI unnoticed until this file made pip a tracked dependency. 26.2.1 is
# clean per https://api.osv.dev/v1/query (PyPI, pip, 26.2.1).
#
# Hashes are pip 26.2.1's published sdist and wheel digests from
# https://pypi.org/pypi/pip/26.2.1/json.
pip==26.2.1 \
    --hash=sha256:71138adf1f4ca900cdb7d289c21b7494329f2332b6d85f0e1c42108c0384ed3e \
    --hash=sha256:f6ad667e89a1fe78046c8f13232b247200f5258d7828f3f7883d660878e0813f
