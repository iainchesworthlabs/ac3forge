# ESP-IDF generates these; sdkconfig.defaults beside them is the committed one.
# The same rules as the esp32s3 project's, which carries the reasoning.

sdkconfig
sdkconfig.old
build-*/
dependencies.lock
managed_components/
