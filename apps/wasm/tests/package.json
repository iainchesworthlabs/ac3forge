{
  "name": "ac3forge-wasm-decode-demo-tests",
  "version": "0.0.0",
  "description": "Headless Playwright coverage for the WASM decode demo (roadmap VX18a)",
  "private": true,
  "type": "commonjs",
  "license": "GPL-3.0-only",
  "scripts": {
    "test": "playwright test",
    "test:device-ui": "playwright test --config device-ui.config.js --project host",
    "coverage:device-ui": "c8 report --temp-directory test-results/device-ui-coverage/tmp --reports-dir test-results/device-ui-coverage --reporter text --reporter lcov --allowExternal --src ../../../esp-idf/ac3forge/ui --all --check-coverage --statements 98 --lines 98 --functions 98 --branches 90",
    "test:board": "playwright test --config device-ui.config.js --project=board board/smoke.spec.js",
    "test:board-layouts": "playwright test --config device-ui.config.js --project=board board/layouts.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "c8": "^12.0.0"
  }
}
