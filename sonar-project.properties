# SonarCloud (SonarQube Cloud) project configuration, used by the nightly scan
# in .github/workflows/sonarcloud.yml. Organisation and project key as the
# project was actually onboarded (confirmed against SonarCloud's own API);
# note this org is NOT the one the sibling aqualink-automate repo analyses
# under, which is `iainchesworth` - the two are separate SonarCloud orgs.
sonar.organization=iainchesworthlabs
sonar.projectKey=iainchesworthlabs_ac3forge
sonar.projectName=ac3forge
sonar.sourceEncoding=UTF-8

# C and C++ files are analysed ONLY when they appear in the compile database
# the workflow passes via sonar.cfamily.compile-commands (the Linux
# config-linux-gcc-coverage build). The Kotlin, TypeScript/JS and Python trees
# need no build, so listing them here is enough.
sonar.sources=src,apps,examples,js,python,tools
sonar.tests=tests

# Trees that never enter a Linux compile database, so the C/C++ analyser would
# skip them with a warning anyway: the Qt GUI, the Windows-only demo app and
# ACX driver, and the NDK / Emscripten / bare-metal C++. Excluded explicitly to
# keep the scan log readable rather than to hide anything - each is built and
# tested by its own CI leg. Kotlin under apps/android IS analysed (it needs no
# compile database), which is why only that tree's cpp/ subdirectory is listed.
#
# docs/assets/wasm-decode-demo/ is a published copy of apps/wasm's demo, byte
# identical to its source - the same reasoning .github/codeql/codeql-config.yml
# gives for ignoring it there.
sonar.exclusions=\
  apps/gui/**,\
  apps/windows/**,\
  apps/android/app/src/main/cpp/**,\
  apps/wasm/**/*.cpp,\
  apps/baremetal/**,\
  docs/assets/wasm-decode-demo/**,\
  build/**,\
  **/node_modules/**,\
  **/*.min.js

# Coverage comes from the ac3tests + ac3cli run only - the same scope
# tools/checks/coverage_report.sh gates on. Everything else is uninstrumented
# and would read as 0% rather than "not measured", which is worse than silence.
sonar.coverage.exclusions=\
  examples/**,\
  js/**,\
  python/**,\
  tools/**,\
  apps/android/**,\
  apps/wasm/**

# Checked-in fixtures and generated tables are not copy-paste defects.
sonar.cpd.exclusions=\
  tests/golden/**,\
  src/forge/src/**/tables*.cpp

# pythonsecurity:S8707/S8701/S8705 ("agentic workflows should not be
# vulnerable to path/command/argument injection") flag every one of these
# tools/ci and tools/checks scripts' own documented CLI contract: a
# `--history`/`--json-out`/`--markdown-out`/positional-path argument used
# as-is for the script's own I/O, or a `--cli`/`--library`/`--elf` argument
# naming which build to invoke, always via list-form subprocess.run (never
# shell=True, never a string command) - already the hardened pattern the
# rule wants. Unlike cpp:S2083's human-CLI-operator case, these rules exist
# because this project's own tooling genuinely IS invoked by AI agents, so
# the threat model was taken seriously and checked file by file (2026-09):
# none of the 39 flagged sites construct a path/command from anything other
# than the caller's own argument, so there is nothing left to validate that
# would not just be re-deriving "trust what the caller who invoked this
# script's stated purpose is to point it at."  A genuinely different
# pattern - e.g. a value taken from parsed FILE CONTENT rather than a CLI
# argument reaching one of these sinks - would not be covered by this
# exclusion and should still surface.
sonar.issue.ignore.multicriteria=e1,e2,e3,e4,e5,e6,e7,e8,e9,e10,e11,e12,e13,e14,e15,e16,e17,e18,e19,e20,e21,e22,e23,e24,e25
sonar.issue.ignore.multicriteria.e1.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e1.resourceKey=tools/ci/append_memory_history.py
sonar.issue.ignore.multicriteria.e2.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e2.resourceKey=tools/ci/check_cli_docs_package.py
sonar.issue.ignore.multicriteria.e3.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e3.resourceKey=tools/ci/check_crucible_package.py
sonar.issue.ignore.multicriteria.e4.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e4.resourceKey=tools/checks/compare_wav.py
sonar.issue.ignore.multicriteria.e5.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e5.resourceKey=tools/checks/derive_channel_floors.py
sonar.issue.ignore.multicriteria.e6.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e6.resourceKey=tools/references/ac4_parse.py
sonar.issue.ignore.multicriteria.e7.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e7.resourceKey=tools/ci/quality_race.py
sonar.issue.ignore.multicriteria.e8.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e8.resourceKey=tools/checks/footprint_report.py
sonar.issue.ignore.multicriteria.e9.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e9.resourceKey=tools/checks/verify_fate_interop.py
sonar.issue.ignore.multicriteria.e10.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e10.resourceKey=tools/ci/compare_performance.py
sonar.issue.ignore.multicriteria.e11.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e11.resourceKey=tools/listening/score_listening_test.py
sonar.issue.ignore.multicriteria.e12.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e12.resourceKey=tools/ci/append_object_quality_history.py
sonar.issue.ignore.multicriteria.e13.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e13.resourceKey=tools/ci/append_external_comparison_history.py
sonar.issue.ignore.multicriteria.e14.ruleKey=pythonsecurity:S8707
sonar.issue.ignore.multicriteria.e14.resourceKey=tools/references/eac3_parse.py
sonar.issue.ignore.multicriteria.e15.ruleKey=pythonsecurity:S8701
sonar.issue.ignore.multicriteria.e15.resourceKey=tools/checks/check_cross_platform_hash.py
sonar.issue.ignore.multicriteria.e16.ruleKey=pythonsecurity:S8701
sonar.issue.ignore.multicriteria.e16.resourceKey=tools/ci/fuzz_eac3_encoder_space.py
sonar.issue.ignore.multicriteria.e17.ruleKey=pythonsecurity:S8701
sonar.issue.ignore.multicriteria.e17.resourceKey=tools/checks/check_matrix_coverage.py
sonar.issue.ignore.multicriteria.e18.ruleKey=pythonsecurity:S8701
sonar.issue.ignore.multicriteria.e18.resourceKey=tools/checks/check_drc.py
sonar.issue.ignore.multicriteria.e19.ruleKey=pythonsecurity:S8701
sonar.issue.ignore.multicriteria.e19.resourceKey=tools/ci/fuzz_encoder_space.py
sonar.issue.ignore.multicriteria.e20.ruleKey=pythonsecurity:S8701
sonar.issue.ignore.multicriteria.e20.resourceKey=tools/ci/quality_race.py
sonar.issue.ignore.multicriteria.e21.ruleKey=pythonsecurity:S8705
sonar.issue.ignore.multicriteria.e21.resourceKey=tools/ci/check_abi_symbols.py
sonar.issue.ignore.multicriteria.e22.ruleKey=pythonsecurity:S8705
sonar.issue.ignore.multicriteria.e22.resourceKey=tools/generators/gen_ac4_baseline.py
sonar.issue.ignore.multicriteria.e23.ruleKey=pythonsecurity:S8705
sonar.issue.ignore.multicriteria.e23.resourceKey=tools/ci/quality_race.py
sonar.issue.ignore.multicriteria.e24.ruleKey=pythonsecurity:S8705
sonar.issue.ignore.multicriteria.e24.resourceKey=tools/checks/footprint_report.py
sonar.issue.ignore.multicriteria.e25.ruleKey=pythonsecurity:S8705
sonar.issue.ignore.multicriteria.e25.resourceKey=tools/checks/verify_fate_interop.py

# NOTE: sonar.cfamily.compile-commands, sonar.cfamily.threads,
# sonar.coverageReportPaths and sonar.qualitygate.wait are set by the workflow,
# not here - they name paths that only exist inside a build, and keeping them
# there means the report list can grow without touching this file.
