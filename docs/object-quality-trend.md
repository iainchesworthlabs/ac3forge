# Object quality trend

Per-object reconstruction quality, by commit. Every push to
`main` encodes one fixed five-object Atmos scene with this build's
`atmos-encode`, decodes it back to per-object WAVs with this build's
`decode`, and scores each recovered object against the source channel it was
built from.

**There is no external oracle for object decode at all, so this is a
self-consistency series.** That is a weaker position than every other trend
page in this project, and the reason is specific rather than incidental.
FFmpeg implements no JOC reconstruction — it decodes these streams as their
5.1 bed and never produces objects to compare against. Dolby's own decoder
does implement it, but [gates object decoding on a keyed authenticity
tag](concepts/atmos-joc.md#two-honest-limitations) this project ships no key
for, so it plays them as the bed too. Nothing outside this repository can
currently produce an independent object decode of an ac3forge stream. What
these numbers say is that this project's encoder and decoder still agree
with each other about TS 103 420 as precisely as they did last commit; a
regression on either side collapses them, and a defect both sides share is
invisible here. Read them as a regression tripwire, not as a conformance
result.

They exist because the object layer had none. Before this page, object
reconstruction was measured exactly once anywhere in the tree — a single
`snr_db > 10.0` assertion in `tests/oba/test_atmos.cpp` against 18–35 dB
measured — while AC-3, E-AC-3 and every Annex E tool each had a per-commit
series. A 15 dB JOC regression passed CI and appeared on no page.

## The scene

`tests/golden/audio/reference_objects.wav` carries five mono object essences
as its five channels, and `reference_objects.paths` places each one in the
room at unit gain. Both are generated by
[`tools/generators/gen_object_scene_wav.py`](https://github.com/iainchesworthlabs/ac3forge/blob/main/tools/generators/gen_object_scene_wav.py),
which documents the scene in full; the cast is borrowed from
`examples/station_broadcast.cpp` because a scene of distinct diegetic sources
is exactly what a parametric object coder has to keep apart.

| Object | What it is | Where it sits |
|---|---|---|
| `broadcast` | a chord-and-formant radio anthem | front centre, static |
| `comet` | band-limited hiss with a slow swell | drifts left to right — the one **moving** object, so the series covers the matrix's per-frame interpolation and not only its steady state |
| `engine` | a low harmonic rumble | rear right, static |
| `pod-hi` | a high whine cluster | rear left, ceiling |
| `pod-lo` | narrow-band chatter | rear left, floor |

`pod-hi` and `pod-lo` are a direction-degenerate pair, and they are in the
scene to probe one thing. The bed has no height channels, so elevation costs
nothing in the downmix and two objects differing only in height are panned
into the five bed channels identically — the limit
[`ac3/oba/atmos.hpp`](https://github.com/iainchesworthlabs/ac3forge/blob/main/src/forge/include/ac3/oba/atmos.hpp)
names in its own module comment. Direction is not the matrix's only axis
though: JOC solves per *frequency band*, so a pair sharing a direction but
not a spectrum is still separable, and these two score as well as the
well-separated objects do. That is what makes their two rows worth reading:
they are the only objects in the scene whose separation rests on band
resolution alone, so a regression that coarsened the banded solve collapses
exactly these two and leaves the rest of the scene reading normally.

Two rates, because they fail differently. At **256 kbit/s** the objects are
reconstructed out of a bed whose own mantissas are the binding constraint; at
**448** (the `AtmosConfig` default) the bed is comfortable and what remains is
the reconstruction limit itself. A bit-allocation regression moves the first
far more than the second; a JOC-matrix regression moves both.

## Regression checks

The same two-tier shape as [Quality trend](quality-trend.md) and
[Tool comparison trend](tool-comparison-trend.md) — a soft tier (0.5 dB below
the trailing 10-run mean for the same leg/object/branch) that only annotates
a row, and a hard tier that fails the CI run that produced it, after the
numbers are still recorded here.

The hard tier is **5 dB**, not the 10 dB the codec series use. Those series
score 21–40 dB; these rows sit at 9.6–22.7 dB, where a 10 dB threshold would
be most of a healthy row's entire headroom. 5 dB is still far more than any
legitimate encoder change moves a row, and still well inside what the defect
class this exists to catch actually costs — a broken reconstruction collapses
a row toward 0 dB, not by a fraction of one. See `REGRESSION_DROP_DB` and
`HARD_REGRESSION_DROP_DB` in
[`tools/ci/append_object_quality_history.py`](https://github.com/iainchesworthlabs/ac3forge/blob/main/tools/ci/append_object_quality_history.py).

<div id="object-trend-app">
  <p class="object-trend-status">Loading object-quality data…</p>
</div>

<style>
#object-trend-app { margin: 1.5em 0; }
.object-trend-status { color: var(--md-default-fg-color--light); font-style: italic; }
.object-trend-controls { display: flex; gap: 1.25em; align-items: center; margin-bottom: 0.75em; flex-wrap: wrap; }
.object-trend-controls label { font-size: 0.85em; color: var(--md-default-fg-color--light); display: inline-flex; align-items: center; gap: 0.35em; white-space: nowrap; }
.object-trend-controls select {
  font: inherit; padding: 0.2em 0.5em; border-radius: 0.2em;
  border: 1px solid var(--md-default-fg-color--lightest);
  background: var(--md-default-bg-color); color: var(--md-default-fg-color);
}
.object-trend-object-filter { display: flex; gap: 0.75em; flex-wrap: wrap; font-size: 0.85em; margin-bottom: 0.75em; }
.object-trend-object-filter label { color: var(--md-default-fg-color--light); display: inline-flex; align-items: center; gap: 0.3em; }
.object-trend-chart-wrap { overflow-x: auto; margin-bottom: 1em; }
.object-trend-chart { display: block; }
.object-trend-legend { display: flex; gap: 1.5em; font-size: 0.85em; margin: 0.5em 0 1em; flex-wrap: wrap; }
.object-trend-legend span { display: inline-flex; align-items: center; gap: 0.4em; }
.object-trend-legend i { width: 0.9em; height: 0.9em; border-radius: 50%; display: inline-block; }
.object-trend-table-wrap { overflow-x: auto; }
#object-trend-app table { width: 100%; border-collapse: collapse; font-size: 0.85em; }
#object-trend-app th, #object-trend-app td { padding: 0.35em 0.6em; text-align: left; border-bottom: 1px solid var(--md-default-fg-color--lightest); white-space: nowrap; }
.object-trend-regression { color: var(--md-typeset-mark-color, #c62828); font-weight: 600; }
.object-trend-release-row { background: color-mix(in srgb, var(--md-accent-fg-color, #7c4dff) 8%, transparent); }
.object-trend-release { text-decoration: none; font-weight: 600; }
.object-trend-release:hover { text-decoration: underline; }
.object-trend-na { color: var(--md-default-fg-color--light); font-style: italic; }
</style>

<script>
(function () {
  const REPO = "iainchesworthlabs/ac3forge";
  const HISTORY_BRANCH = "quality-history";
  const TRACKS = [
    { branch: "develop", color: "#7c4dff" },
    { branch: "main", color: "#00acc1" },
  ];
  // Mirrors tools/ci/append_object_quality_history.py's own constants - keep
  // these in sync if that script's thresholds change; this is a display-only
  // echo, not a second source of truth. Only the soft (non-failing) tier is
  // shown here - a hard regression fails its own CI run directly, so it does
  // not need a table annotation to be noticed too.
  const REGRESSION_WINDOW = 10;
  const REGRESSION_DROP_DB = 0.5;
  const TABLE_ROWS = 40;
  const LEGS = ["atmos-objects-256", "atmos-objects-448"];
  // "scene" is the mean of the five objects, not a sixth object - see
  // race_objects() in tools/ci/quality_race.py. It leads the list because it
  // is the default chart line: one number per commit for "did the object
  // layer move", with the five that make it up available beside it.
  const ALL_OBJECTS = ["scene", "broadcast", "comet", "engine", "pod-hi", "pod-lo"];
  // Fixed color per object, same reasoning as the sibling trend pages': a
  // line keeps its color across renders rather than shuffling with whichever
  // rows happen to be selected.
  const OBJECT_COLORS = {
    scene: "#7c4dff",
    broadcast: "#00acc1",
    comet: "#43a047",
    engine: "#fb8c00",
    "pod-hi": "#e53935",
    "pod-lo": "#3949ab",
  };
  const DEFAULT_TABLE_OBJECTS = ["scene", "comet"];

  const root = document.getElementById("object-trend-app");

  const state = {
    leg: "atmos-objects-448",
    // "branch": one line per branch for a single focus object (the default).
    // "object": one line per object for a single branch, un-folded. Never
    // both as line dimensions at once - the same tradeoff the sibling trend
    // pages make, for the same readability reason.
    chartMode: "branch",
    chartObject: "scene",
    chartBranch: "develop",
    tableObjects: Object.fromEntries(ALL_OBJECTS.map((o) => [o, DEFAULT_TABLE_OBJECTS.includes(o)])),
    branches: { main: true, develop: true },
    developFullHistory: false,
  };

  function rawUrl(branch, file) {
    return `https://raw.githubusercontent.com/${REPO}/${branch}/${file}`;
  }

  function parseJsonl(text) {
    return text.split("\n").filter((l) => l.trim().length > 0).map((l) => JSON.parse(l));
  }

  async function fetchTrack(branch) {
    try {
      const resp = await fetch(rawUrl(HISTORY_BRANCH, `object-quality-${branch}.jsonl`));
      if (!resp.ok) return [];
      return parseJsonl(await resp.text());
    } catch (e) {
      return [];
    }
  }

  // Same client-side, best-effort tag->release join as quality-trend.md -
  // see that page's identical function for the full reasoning.
  async function fetchReleaseShaMap() {
    let shaMap = {};
    try {
      const resp = await fetch(`https://api.github.com/repos/${REPO}/tags?per_page=100`);
      if (!resp.ok) return shaMap;
      const tags = await resp.json();
      for (const t of tags) {
        if (t.commit && t.commit.sha) shaMap[t.commit.sha] = { tag: t.name, name: t.name, url: `https://github.com/${REPO}/releases/tag/${t.name}` };
      }
    } catch (e) {
      return shaMap;
    }
    try {
      const resp = await fetch(`https://api.github.com/repos/${REPO}/releases?per_page=100`);
      if (resp.ok) {
        const releases = await resp.json();
        const byTag = {};
        for (const rel of releases) byTag[rel.tag_name] = rel;
        for (const sha of Object.keys(shaMap)) {
          const rel = byTag[shaMap[sha].tag];
          if (rel) {
            shaMap[sha].name = rel.name || shaMap[sha].tag;
            shaMap[sha].prerelease = !!rel.prerelease;
            shaMap[sha].url = rel.html_url || shaMap[sha].url;
          }
        }
      }
    } catch (e) {
      // Tag->sha map still usable without release metadata.
    }
    return shaMap;
  }

  function shortSha(sha) {
    return sha.slice(0, 8);
  }

  function commitUrl(sha) {
    return `https://github.com/${REPO}/commit/${sha}`;
  }

  function mostRecentCommit(records) {
    if (records.length === 0) return null;
    return records.reduce((a, b) => (a.commit_date > b.commit_date ? a : b)).commit;
  }

  // "object" mode always looks at one branch's full leg history - the point
  // is a per-object trend over many commits, so the develop-collapse and
  // other-branch filters "branch" mode uses do not apply there.
  function visibleRecords(allRecords) {
    const legRecords = allRecords.filter((r) => r.leg === state.leg);
    if (state.chartMode === "object") {
      return legRecords.filter((r) => r.branch === state.chartBranch);
    }
    const latestDevelop = mostRecentCommit(legRecords.filter((r) => r.branch === "develop"));
    return legRecords.filter((r) => {
      if (!state.branches[r.branch]) return false;
      if (r.branch === "develop" && !state.developFullHistory) {
        return r.commit === latestDevelop;
      }
      return true;
    });
  }

  function chartSeries(records, object) {
    return records
      .filter((r) => r.variant === object)
      .slice()
      .sort((a, b) => a.commit_date.localeCompare(b.commit_date));
  }

  // Always against the full, unfiltered leg history - the display collapse
  // should not change what counts as a regression.
  function regressionBaseline(allLegRecords, object, branch, beforeCommitDate) {
    const trail = allLegRecords
      .filter((r) => r.variant === object && r.branch === branch && r.commit_date < beforeCommitDate)
      .sort((a, b) => a.commit_date.localeCompare(b.commit_date))
      .slice(-REGRESSION_WINDOW);
    if (trail.length === 0) return null;
    return trail.reduce((sum, r) => sum + r.snr_db, 0) / trail.length;
  }

  function buildChart(tracks, releasesBySha) {
    const width = 760, height = 220, pad = { top: 12, right: 12, bottom: 32, left: 42 };
    const allPoints = tracks.flatMap((t) => t.points);
    if (allPoints.length === 0) {
      return state.chartMode === "object"
        ? `<p class="object-trend-status">No history for ${state.leg} on ${state.chartBranch} in the current view.</p>`
        : `<p class="object-trend-status">No "${state.chartObject}" history for ${state.leg} in the current view.</p>`;
    }
    const dbValues = allPoints.map((p) => p.snr_db);
    const minDb = Math.min(...dbValues) - 2;
    const maxDb = Math.max(...dbValues) + 2;
    const times = allPoints.map((p) => Date.parse(p.commit_date));
    const minT = Math.min(...times);
    const maxT = Math.max(...times);

    const x = (t) => pad.left + (maxT === minT ? (width - pad.left - pad.right) / 2 : ((t - minT) / (maxT - minT)) * (width - pad.left - pad.right));
    const y = (db) => height - pad.bottom - ((db - minDb) / (maxDb - minDb)) * (height - pad.top - pad.bottom);

    const chartLabel = state.chartMode === "object"
      ? `Object SNR by commit date, ${state.leg} on ${state.chartBranch}, by object`
      : `Object SNR by commit date, ${state.leg} ${state.chartObject}`;
    let svg = `<svg class="object-trend-chart" viewBox="0 0 ${width} ${height}" width="${width}" height="${height}" role="img" aria-label="${chartLabel}">`;
    for (let i = 0; i <= 4; i++) {
      const db = minDb + ((maxDb - minDb) * i) / 4;
      const gy = y(db);
      svg += `<line x1="${pad.left}" y1="${gy}" x2="${width - pad.right}" y2="${gy}" stroke="var(--md-default-fg-color--lightest)" stroke-width="1"/>`;
      svg += `<text x="${pad.left - 6}" y="${gy + 3}" text-anchor="end" font-size="10" fill="var(--md-default-fg-color--light)">${db.toFixed(0)}</text>`;
    }
    svg += `<text x="${pad.left}" y="${height - 8}" text-anchor="start" font-size="10" fill="var(--md-default-fg-color--light)">${new Date(minT).toISOString().slice(0, 10)}</text>`;
    svg += `<text x="${width - pad.right}" y="${height - 8}" text-anchor="end" font-size="10" fill="var(--md-default-fg-color--light)">${new Date(maxT).toISOString().slice(0, 10)}</text>`;

    for (const track of tracks) {
      const pts = track.points;
      if (pts.length === 0) continue;
      if (pts.length > 1) {
        const path = pts.map((p, i) => `${i === 0 ? "M" : "L"}${x(Date.parse(p.commit_date)).toFixed(1)},${y(p.snr_db).toFixed(1)}`).join(" ");
        svg += `<path d="${path}" fill="none" stroke="${track.color}" stroke-width="2"/>`;
      }
      pts.forEach((p) => {
        const cx = x(Date.parse(p.commit_date)).toFixed(1);
        const cy = y(p.snr_db).toFixed(1);
        const release = releasesBySha[p.commit];
        const title = `${track.label} ${shortSha(p.commit)} - ${p.snr_db.toFixed(2)} dB on ${p.commit_date.slice(0, 10)}${release ? ` - release ${release.name}` : ""}`;
        svg += `<circle cx="${cx}" cy="${cy}" r="3" fill="${track.color}"><title>${title}</title></circle>`;
        if (release) {
          svg += `<circle cx="${cx}" cy="${cy}" r="6.5" fill="none" stroke="${track.color}" stroke-width="1.5" stroke-dasharray="2,1.5"><title>${title}</title></circle>`;
        }
      });
    }
    svg += "</svg>";
    return svg;
  }

  function buildLegend(tracks, releasesBySha) {
    const items = tracks.filter((t) => t.points.length > 0).map((t) => `<span><i style="background:${t.color}"></i>${t.label}</span>`);
    const anyRelease = tracks.some((t) => t.points.some((p) => releasesBySha[p.commit]));
    if (anyRelease) {
      items.push('<span><i style="background:none;border:1.5px dashed var(--md-default-fg-color--light);"></i>tagged release</span>');
    }
    return `<div class="object-trend-legend">${items.join("")}</div>`;
  }

  function buildTable(visible, allLegRecords, releasesBySha) {
    const rows = visible.filter((r) => state.tableObjects[r.variant]);
    const trs = rows
      .slice()
      .sort((a, b) => b.commit_date.localeCompare(a.commit_date))
      .slice(0, TABLE_ROWS)
      .map((r) => {
        const baseline = regressionBaseline(allLegRecords, r.variant, r.branch, r.commit_date);
        const regressed = baseline !== null && baseline - r.snr_db >= REGRESSION_DROP_DB;
        const flag = regressed
          ? `<span class="object-trend-regression" title="${(baseline - r.snr_db).toFixed(2)} dB below the trailing ${REGRESSION_WINDOW}-run mean (${baseline.toFixed(2)} dB)">▼ regression</span>`
          : "";
        const release = releasesBySha[r.commit];
        const releaseBadge = release
          ? `<a class="object-trend-release" href="${release.url}" title="${release.prerelease ? "Prerelease" : "Release"} tagged at this commit">🏷 ${release.name}</a>`
          : "";
        const leak = r.leak_db == null
          ? '<span class="object-trend-na" title="This object occupies every band, so there is nowhere outside it for anything to leak into and no measurement to report">n/a</span>'
          : `${r.leak_db.toFixed(1)} dB`;
        return `<tr${release ? ' class="object-trend-release-row"' : ""}>
          <td>${r.commit_date.slice(0, 10)}</td>
          <td>${r.branch}</td>
          <td><a href="${commitUrl(r.commit)}">${shortSha(r.commit)}</a></td>
          <td>${r.variant}</td>
          <td>${r.snr_db.toFixed(2)} dB</td>
          <td>${r.lsd_db == null ? "-" : r.lsd_db.toFixed(2) + " dB"}</td>
          <td>${leak}</td>
          <td>${r.mos_lqo == null ? "-" : r.mos_lqo.toFixed(2)}</td>
          <td>${releaseBadge}</td>
          <td>${flag}</td>
        </tr>`;
      })
      .join("");
    if (trs === "") {
      return '<p class="object-trend-status">No rows in the current view - try a different leg/branch/object combination.</p>';
    }
    return `<div class="object-trend-table-wrap"><table>
      <thead><tr><th>Date</th><th>Branch</th><th>Commit</th><th>Object</th><th>SNR</th><th>LSD</th><th>Leakage</th><th>MOS</th><th>Release</th><th></th></tr></thead>
      <tbody>${trs}</tbody>
    </table></div>`;
  }

  function buildControls() {
    const objectMode = state.chartMode === "object";
    return `
      <div class="object-trend-controls">
        <label for="object-trend-leg">Leg
          <select id="object-trend-leg">
            ${LEGS.map((l) => `<option value="${l}" ${state.leg === l ? "selected" : ""}>${l}</option>`).join("")}
          </select>
        </label>
        <label for="object-trend-chart-mode">Chart
          <select id="object-trend-chart-mode">
            <option value="branch" ${!objectMode ? "selected" : ""}>By branch</option>
            <option value="object" ${objectMode ? "selected" : ""}>By object</option>
          </select>
        </label>
        ${objectMode ? `
          <label for="object-trend-chart-branch">Branch
            <select id="object-trend-chart-branch">
              <option value="develop" ${state.chartBranch === "develop" ? "selected" : ""}>develop</option>
              <option value="main" ${state.chartBranch === "main" ? "selected" : ""}>main</option>
            </select>
          </label>
        ` : `
          <label for="object-trend-chart-object">Chart line
            <select id="object-trend-chart-object">
              ${ALL_OBJECTS.map((o) => `<option value="${o}" ${state.chartObject === o ? "selected" : ""}>${o}</option>`).join("")}
            </select>
          </label>
          <label><input type="checkbox" id="object-trend-branch-main" ${state.branches.main ? "checked" : ""}/> main</label>
          <label><input type="checkbox" id="object-trend-branch-develop" ${state.branches.develop ? "checked" : ""}/> develop</label>
          ${state.branches.develop ? `<label><input type="checkbox" id="object-trend-develop-history" ${state.developFullHistory ? "checked" : ""}/> develop: show full history</label>` : ""}
        `}
      </div>
      <div class="object-trend-object-filter">
        <span>Table rows:</span>
        ${ALL_OBJECTS.map((o) => `<label><input type="checkbox" class="object-trend-object-checkbox" data-object="${o}" ${state.tableObjects[o] ? "checked" : ""}/> ${o}</label>`).join("")}
      </div>
    `;
  }

  function attachControlListeners(allRecords, releasesBySha) {
    document.getElementById("object-trend-leg").addEventListener("change", (e) => {
      state.leg = e.target.value;
      render(allRecords, releasesBySha);
    });
    document.getElementById("object-trend-chart-mode").addEventListener("change", (e) => {
      state.chartMode = e.target.value;
      render(allRecords, releasesBySha);
    });
    const chartBranchSelect = document.getElementById("object-trend-chart-branch");
    if (chartBranchSelect) {
      chartBranchSelect.addEventListener("change", (e) => {
        state.chartBranch = e.target.value;
        render(allRecords, releasesBySha);
      });
    }
    const chartObjectSelect = document.getElementById("object-trend-chart-object");
    if (chartObjectSelect) {
      chartObjectSelect.addEventListener("change", (e) => {
        state.chartObject = e.target.value;
        render(allRecords, releasesBySha);
      });
    }
    const mainToggle = document.getElementById("object-trend-branch-main");
    if (mainToggle) {
      mainToggle.addEventListener("change", (e) => {
        state.branches.main = e.target.checked;
        render(allRecords, releasesBySha);
      });
    }
    const developToggle = document.getElementById("object-trend-branch-develop");
    if (developToggle) {
      developToggle.addEventListener("change", (e) => {
        state.branches.develop = e.target.checked;
        render(allRecords, releasesBySha);
      });
    }
    const historyToggle = document.getElementById("object-trend-develop-history");
    if (historyToggle) {
      historyToggle.addEventListener("change", (e) => {
        state.developFullHistory = e.target.checked;
        render(allRecords, releasesBySha);
      });
    }
    document.querySelectorAll(".object-trend-object-checkbox").forEach((cb) => {
      cb.addEventListener("change", (e) => {
        state.tableObjects[e.target.dataset.object] = e.target.checked;
        render(allRecords, releasesBySha);
      });
    });
  }

  function buildTracks(visible) {
    if (state.chartMode === "object") {
      return ALL_OBJECTS.map((o) => ({
        key: o,
        label: o,
        color: OBJECT_COLORS[o],
        points: chartSeries(visible, o),
      }));
    }
    return TRACKS.map((t) => ({
      key: t.branch,
      label: t.branch === "develop" && !state.developFullHistory ? `${t.branch} (latest commit only)` : t.branch,
      color: t.color,
      points: state.branches[t.branch] ? chartSeries(visible.filter((r) => r.branch === t.branch), state.chartObject) : [],
    }));
  }

  function render(allRecords, releasesBySha) {
    const allLegRecords = allRecords.filter((r) => r.leg === state.leg);
    const visible = visibleRecords(allRecords);
    const tracks = buildTracks(visible);
    root.innerHTML = `
      ${buildControls()}
      <div class="object-trend-chart-wrap">${buildChart(tracks, releasesBySha)}</div>
      ${buildLegend(tracks, releasesBySha)}
      ${buildTable(visible, allLegRecords, releasesBySha)}
    `;
    attachControlListeners(allRecords, releasesBySha);
  }

  Promise.all([...TRACKS.map((t) => fetchTrack(t.branch)), fetchReleaseShaMap()]).then((results) => {
    const releasesBySha = results.pop();
    const allRecords = [];
    TRACKS.forEach((t, i) => allRecords.push(...results[i]));
    if (allRecords.length === 0) {
      root.innerHTML = '<p class="object-trend-status">No object-quality history yet - it is written by CI on the first push to main after this page landed.</p>';
      return;
    }
    render(allRecords, releasesBySha);
  });
})();
</script>

## Reading it

Each row is one (commit, leg, object) result. The **Chart** control picks
what the lines mean, always within the leg selected above. "By branch" (the
default) plots one focus object — `scene` by default, via the "Chart line"
selector — as one point per commit per branch. "By object" un-folds a single
branch into one line per object instead, which is how a single object
drifting away from its four siblings becomes visible as its own line rather
than something you would only find by cycling the focus selector.

`develop` and `main` behave as on the sibling pages: separate tracks
(`main` only advances on a release promotion), `develop` collapsed to its
latest commit by default, and a 🏷 badge on a row whose commit was tagged.

**`scene`** is the plain mean of that leg's five objects, not a sixth object.
It is the "did the object layer move" number; an object that collapsed on its
own arrives here diluted by four that did not, which is exactly why the
per-object rows sit beside it rather than instead of it.

Four metrics, and only SNR is regression-checked:

- **SNR** — the recovered object against its source channel, delay-compensated
  by 832 samples. That figure is not a fudge factor: `joc::reconstruct` is a
  *delayed* identity, carrying two stacked delays — 256 samples from the
  bed's own encode/decode, plus 576 more from reconstruction's own pass over
  that decoded bed through §7.1's 64-band complex QMF filterbank (the
  default `Domain::kQmf`; `ac3::dsp::kQmfDelay` in `qmf.hpp`), not the
  older 256-sample MDCT round trip `Domain::kMdctBand` used before the
  filterbank existed. `ac3::joc::reconstruction_delay(domain)` is the single
  place either figure is derived, and `tests/oba/test_atmos.cpp` calls it
  rather than hard-coding a number; its comment carries the full reasoning.
  It is fixed rather than found by cross-correlation on purpose — a
  correlation search would silently absorb a real timing regression as
  "just a different lag" instead of reporting it as the SNR collapse it is.
- **LSD** — banded log-spectral distance, restricted to the Bark bands the
  object actually occupies. Objects are individually narrow-band by nature
  (this scene's engine is 58–232 Hz and occupies 3 of the 24 bands; only the
  broadband `comet` occupies all of them), so most bands hold nothing but the
  reference's own numerical floor and the unrestricted measure the codec legs
  use reports a *healthy* reconstruction here at 10–38 dB. See
  `object_spectral_scores` in `tools/ci/quality_race.py`.
- **Leakage** — all the coded energy that landed outside those bands, against
  all the reference energy inside them. This is the object-specific measure:
  the audible failure of a JOC reconstruction is another object's content
  arriving in this one. Lower is better. **n/a** on an object occupying every
  band — this scene's `comet`, a broadband hiss — where there is nowhere
  outside for anything to leak into and so no measurement, not a zero.
- **MOS** — [ViSQOL](https://github.com/google/visqol)'s MOS-LQO, the same
  optional perceptual column as the sibling pages, showing `-` on any run
  whose environment lacked `visqol-python`. See
  [Tool comparison trend](tool-comparison-trend.md#reading-it) for why ViSQOL
  over PEAQ.

## Where the data lives

Same `quality-history` branch mechanism as
[Quality trend](quality-trend.md#where-the-data-lives) and its siblings —
`object-quality-<branch>.jsonl` this time, written by a job in `ci.yml`
(`persist-object-quality-trend`) downstream of `ffmpeg-validate`'s
compute-only `objects` step, on direct pushes to `main` only.

Reproduce any row locally, after building `ac3cli`:

```bash
AC3CLI=build/config-linux-llvm/bin/ac3cli python3 tools/ci/quality_race.py objects
```
