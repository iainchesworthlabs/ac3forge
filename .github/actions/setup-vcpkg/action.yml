name: Setup vcpkg
description: >-
  Clone and bootstrap vcpkg at a pinned commit, and wire up a cached binary +
  download cache. ac3forge does not vendor vcpkg as a submodule (unlike
  aqualink-automate), so the checkout happens here rather than in the repo.

inputs:
  vcpkg-commit:
    description: >-
      microsoft/vcpkg commit to pin. This is what fixes the dependency
      versions: vcpkg.json carries no builtin-baseline, so the registry
      baseline comes from the checkout's own git history.
    required: true
  cache-key-suffix:
    description: >-
      Extra archives-cache key component for jobs whose install set differs
      from the default manifest's - pass the manifest FEATURES the job
      enables (e.g. "adm"). Without this, every Linux job shares one key,
      and GitHub cache entries are IMMUTABLE: whichever job saves a key
      first locks its (possibly much smaller) archive set in for everyone -
      found the hard way when a boost-less job saved the post-fmt-migration
      key and the adm job's 43 boost ports could then never be added,
      forcing a full boost rebuild (and its codeload-400 exposure) on every
      adm-validate run forever. A distinct key per feature set lets each
      install shape save its own complete entry.
    default: ""

runs:
  using: composite
  steps:
    # A full clone: manifest mode with no builtin-baseline resolves the baseline
    # from the checkout's history, which a --depth 1 clone does not have. Then
    # reset to the pin so a rebuild months from now installs the same Catch2.
    - name: Clone and bootstrap vcpkg
      shell: bash
      # inputs.vcpkg-commit read through env: rather than interpolated
      # straight into the script text - every caller today passes a literal
      # SHA, but a value substituted directly into a run: block is shell CODE,
      # not data, so this closes that off rather than relying on every future
      # caller staying literal (zizmor: template-injection).
      #
      # zizmor's github-env audit also flags both `echo VCPKG_ROOT=... >>
      # $GITHUB_ENV` writes below: it can't statically tell a safe dynamic
      # value from an attacker-controlled one, so it flags every non-literal
      # write on principle. $VCPKG_DIR here is built from $RUNNER_TEMP (a
      # runner-assigned path) plus the literal suffix "/vcpkg" - nothing in it
      # comes from a PR title, branch name, issue body or other
      # attacker-reachable string, on this or any workflow that calls this
      # composite action. VCPKG_ROOT has to be a real environment variable
      # for CMake's vcpkg toolchain file to pick it up automatically in every
      # later step across every job that uses this action - threading it
      # through $GITHUB_OUTPUT instead would mean every one of those steps
      # re-exporting it by hand, for no real safety gain here.
      env:
        VCPKG_COMMIT: ${{ inputs.vcpkg-commit }}
      run: | # zizmor: ignore[github-env] VCPKG_ROOT is RUNNER_TEMP-derived, not attacker-influenced - see above
        set -euo pipefail
        VCPKG_DIR="${RUNNER_TEMP}/vcpkg"
        if [ ! -d "$VCPKG_DIR/.git" ]; then
          git clone https://github.com/microsoft/vcpkg.git "$VCPKG_DIR"
        fi
        git -C "$VCPKG_DIR" fetch --tags origin
        git -C "$VCPKG_DIR" checkout --force "$VCPKG_COMMIT"
        if [ "$RUNNER_OS" = "Windows" ]; then
          "$VCPKG_DIR/bootstrap-vcpkg.bat" -disableMetrics
          echo "VCPKG_ROOT=$(cygpath -w "$VCPKG_DIR")" >> "$GITHUB_ENV"
        else
          "$VCPKG_DIR/bootstrap-vcpkg.sh" -disableMetrics
          echo "VCPKG_ROOT=$VCPKG_DIR" >> "$GITHUB_ENV"
        fi

    # vcpkg rejects an MSYS-style path (/c/...) for these, so hand it a native
    # Windows path. The cache action below still needs the bash-style path,
    # hence the two forms.
    #
    # Same github-env reasoning as the step above: BINARY_DIR/DOWNLOADS_DIR
    # are $HOME plus literal suffixes, optionally reshaped by cygpath - never
    # attacker-influenced. VCPKG_DEFAULT_BINARY_CACHE/VCPKG_DOWNLOADS are
    # vcpkg's own env-var configuration knobs, read implicitly by every vcpkg
    # invocation in later steps.
    - name: Set up vcpkg cache directories
      shell: bash
      run: | # zizmor: ignore[github-env] BINARY_DIR/DOWNLOADS_DIR are $HOME-derived, not attacker-influenced - see above
        set -euo pipefail
        BINARY_DIR="$HOME/.cache/vcpkg/archives"
        DOWNLOADS_DIR="$HOME/.cache/vcpkg/downloads"
        mkdir -p "$BINARY_DIR" "$DOWNLOADS_DIR"
        if command -v cygpath >/dev/null 2>&1; then
          BINARY_DIR=$(cygpath -w "$BINARY_DIR")
          DOWNLOADS_DIR=$(cygpath -w "$DOWNLOADS_DIR")
        fi
        echo "VCPKG_DEFAULT_BINARY_CACHE=$BINARY_DIR" >> "$GITHUB_ENV"
        echo "VCPKG_DOWNLOADS=$DOWNLOADS_DIR" >> "$GITHUB_ENV"
        echo "VCPKG_BINARY_SOURCES=clear;default,readwrite" >> "$GITHUB_ENV"

    # Keyed by OS + arch + the vcpkg pin + the manifest. Compiler is deliberately
    # NOT in the key: the two Linux legs (gcc, clang) and the two Windows legs
    # (cl, clang-cl) build the same x64-linux / x64-windows triplet, so they
    # share one cache instead of duplicating it. vcpkg names archives by ABI
    # hash, so entries for different triplets coexist safely anyway.
    - name: Cache vcpkg binaries
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: ~/.cache/vcpkg/archives
        key: vcpkg-${{ runner.os }}-${{ runner.arch }}-${{ inputs.vcpkg-commit }}-${{ hashFiles('vcpkg.json') }}${{ inputs.cache-key-suffix != '' && format('-{0}', inputs.cache-key-suffix) || '' }}
        restore-keys: |
          vcpkg-${{ runner.os }}-${{ runner.arch }}-${{ inputs.vcpkg-commit }}-${{ hashFiles('vcpkg.json') }}
          vcpkg-${{ runner.os }}-${{ runner.arch }}-${{ inputs.vcpkg-commit }}-
          vcpkg-${{ runner.os }}-${{ runner.arch }}-

    # runner.arch in the key, matching the binaries cache above - found missing
    # for real bringing up the arm64 Linux legs: without it, an arm64 runner's
    # restore-keys fallback can match a downloads cache an x64 runner
    # populated (or vice versa), silently handing vcpkg the wrong architecture's
    # previously-downloaded tool binaries for anything not covered by an exact
    # key hit.
    - name: Cache vcpkg downloads
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: ~/.cache/vcpkg/downloads
        key: vcpkg-downloads-${{ runner.os }}-${{ runner.arch }}-${{ inputs.vcpkg-commit }}-${{ hashFiles('vcpkg.json') }}${{ inputs.cache-key-suffix != '' && format('-{0}', inputs.cache-key-suffix) || '' }}
        restore-keys: |
          vcpkg-downloads-${{ runner.os }}-${{ runner.arch }}-${{ inputs.vcpkg-commit }}-${{ hashFiles('vcpkg.json') }}
          vcpkg-downloads-${{ runner.os }}-${{ runner.arch }}-${{ inputs.vcpkg-commit }}-
          vcpkg-downloads-${{ runner.os }}-${{ runner.arch }}-
